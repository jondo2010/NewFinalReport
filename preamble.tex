%
%	preamble.tex
%
%	Final Report: Document Preamble
%
%	John Hughes and Michael Jean
%	University of Manitoba
%

\usepackage[T1]{fontenc}
%\usepackage[latin9]{inputenc}
\usepackage[utf8x]{inputenc}

\setcounter{secnumdepth}{3}
\setcounter{tocdepth}{3}

\usepackage{array}
\usepackage{babel}
\usepackage{float}
\usepackage[letterpaper]{geometry}
\usepackage{graphicx}
\usepackage{graphics}
\usepackage{lastpage}
\usepackage{tikz}
\usepackage{tikz-timing}[2009/05/15]
%\usetikztiminglibrary{nicetabs}
%\tikzset{timing/no nice tabs}
%\tikzset{timing/interval/normal}
%\tikzset{timing/nodes/old center}
\usepackage{subfigure}
\usepackage{tabularx}
\usepackage{array}
\usepackage{flafter,fnpos}
\usepackage{booktabs}
\usepackage{supertabular}

%\usepackage[square,authoryear,sort&compress]{natbib}
\usepackage[numbers, square, comma, sort&compress]{natbib}

\usepackage{nomencl}
\makenomenclature

%\usepackage[automark,autooneside,markuppercase]{scrpage2}
%\pagestyle{scrheadings}
%\lehead{Distributed Sensor/Actuator Network}
%\lohead{Distributed Sensor/Actuator Network}
%\ihead{\headmark}
%\automark[section]{chapter}
%\setheadsepline{.4pt}

\usepackage{fancyhdr}
\pagestyle{fancy}

%%% Set Margins
\setlength{\hoffset}{0.5in}
\oddsidemargin=0pt
\setlength{\topmargin}{0pt}
\setlength{\marginparwidth}{0pt}
\setlength{\marginparsep}{0pt}
\setlength{\textwidth}{6in}

%\setlength{\parskip}{12 pt}
\setlength{\parskip}{\medskipamount}
\makeatletter

%%% Add the running title to the left side of the header
\fancyhead[L]{Distributed Sensor/Actuator Network}

%%% Renew the chaptermark command to display CH. instead of CHAPTER in the header
\renewcommand{\chaptermark}[1]{\markboth{\MakeUppercase{CH. \thechapter. #1}}{}}

%%% Renew the pagemark command to add dashes to either side of the page numbers
\renewcommand{\pagemark}{--\hspace{5pt}{\thepage}\hspace{5pt}--}

%%% Page numbers and date to the bottom footer
\fancyfoot[C]{--\hspace{5pt}{\thepage}\hspace{5pt}--}
\fancyfoot[R]{\tiny{\jobname{.tex}\\ \today}}

%%% Shorten the TOC depth
\setcounter{tocdepth}{2}

\input{tikz_styles}

\usepackage[
  unicode=true,
  pdfusetitle,
  bookmarks=true,
  bookmarksnumbered=true,
  bookmarksopen=false,
  breaklinks=false,
  pdfborder={0 0 0},
  backref=false,
  colorlinks=false
] {hyperref}